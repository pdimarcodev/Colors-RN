import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 300,
    height: 200,
  },
  image: {
    flex: 1,
    borderRadius: 18,
    borderWidth: 1,
    borderColor: 'grey',
  },
});
